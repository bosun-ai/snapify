<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="{{ 'main.css' | asset_url }}" data-test="direct-href">
    <script src="{{ 'app.js' | asset_url }}" data-test="direct-script" defer="defer"></script>
    {{ content_for_header }}
  </head>
  <body>
    {% section 'global-assets' %}
    <main id="MainContent">
      {{ content_for_layout }}
    </main>
  </body>
</html>
